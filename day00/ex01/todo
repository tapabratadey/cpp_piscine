-only 8 contacts can be added. 9th contact will be error
-contacts start empty
-if EXIT, 
    delete all the contacts and exit program
-else if ADD, 
    -prompt the user to add new contact info one at a time
    
    -first name
    -last name
    -nickname
    -login
    -postal address
    -email address
    -phone number
    -birthdate
    -favorite meal
    -underwear color
    -darkest secret
    
    -contact must be an instance of a class.
-else if SEARCH,
    -